---------------------------------------------------------------------------------------------------------------------------------

To compile all executables and store in the ../exec subdirectory: 

makes <mach>

where <mach> is one of the included platforms:

wcoss
zeus
gaea
jibb

Each sorc subdirectory contain a makes script which operates the same way except Rmaking only the code(s) in that subdirectory.

---------------------------------------------------------------------------------------------------------------------------------
